[project]
name = "riskgate-test-utilities"
version = "0.1.0"
description = "Shared test fixtures and helpers"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/riskgate_test_utilities"]
